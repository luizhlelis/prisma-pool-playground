#pgbouncer.ini

[databases]
* = host = postgres port=5432 user=prisma_user password=prisma_password dbname=heroes_vs_enemies

[pgbouncer]
listen_addr = 0.0.0.0
auth_type = any
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts

